# detail.md Milestone 4.4: scrape the control plane's /actuator/prometheus.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: dtunnel-control-plane
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["control-plane:8080"]
        labels:
          service: dtunnel-control-plane
